Q1. Double Post-Increment Loop
#include <stdio.h>
int main() {
int i = 0;
while (i++ < 4)
printf("%d ", ++i);
return 0;
}
Task: Explain the control flow and predict output.

Q2. Factorial Calculation
Task: Calculate factorial of a number using recursion.
Code (C):
#include <stdio.h>
int factorial(int n) {
    if(n==0) return 1;
    else return n*factorial(n-1);
}
int main() {
    int n;
    printf("Enter number: "); scanf("%d", &n);
    printf("Factorial = %d", factorial(n));
    return 0;
}


Q3. Two-Dimensional Indexing via Pointer
#include <stdio.h>
int main() {
int m[2][3] = {{1,2,3},{4,5,6}};
printf("%d %d\n", *(*(m + 1) + 2), m[0][1]);
return 0;
}
Task: Explain the pointer arithmetic to access matrix elements.
