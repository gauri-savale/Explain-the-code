Q1. For-Loop with Empty Body
#include <stdio.h>
int main() {
int i;
for (i = 0; i < 3; i++);
printf("%d\n", i);
return 0;
}
Task: Explain why i has that final value.


Q2. Pointer Alias Mutation
#include <stdio.h>
int main() {
int x = 3, y = 4;
int *p = &x, *q = &y;
*p = *q + 2;
q = p;
*q = *q + 1;
printf("%d %d\n", x, y);
return 0;
}
Task: Explain each pointer assignment and final printed values.

Q3. Prime Number Check
Task: Check if number is prime.
Code (C):
#include <stdio.h>
int main() {
    int n,i,flag=0;
    printf("Enter number: "); scanf("%d",&n);
    for(i=2;i<=n/2;i++){
        if(n%i==0){ flag=1; break; }
    }
    if(flag||n<=1) printf("Not prime");
    else printf("Prime");
    return 0;
}

