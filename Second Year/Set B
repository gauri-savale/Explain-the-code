Q1. Pointer Alias Mutation
#include <stdio.h>
int main() {
int x = 3, y = 4;
int *p = &x, *q = &y;
*p = *q + 2;
q = p;
*q = *q + 1;
printf("%d %d\n", x, y);
return 0;
}
Task: Explain each pointer assignment and final printed values.

Q2. Iterator Exhaustion Pitfall
it = iter([1,2,3])
print(next(it))
print(list(it))
print(next(it))
Task: Describe outputs and the error (if any).

Q3. DP Table Build — Longest Common Subsequence
A = "AGGTAB"
B = "GXTXAYB"
n, m = len(A), len(B)
dp = [[0]*(m+1) for _ in range(n+1)]
for i in range(1, n+1):
for j in range(1, m+1):
if A[i-1] == B[j-1]:
dp[i][j] = dp[i-1][j-1] + 1
else:
dp[i][j] = max(dp[i-1][j], dp[i][j-1])
print(dp[n][m])
Task: Explain DP recurrence and printed value.
